services:
    simple_user.subscriber:
        class: SimpleUser\Events\UserSubscriber
        arguments: '@security.password_encoder'
        tags:
          - { name: doctrine.event_subscriber, connection: default }
